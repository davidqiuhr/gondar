version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - git submodule update --init
      - docker build --file docker/gondar-linux.Dockerfile .
  post_build:
    commands:
      - echo Build completed on `date`
