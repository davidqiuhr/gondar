version: 0.2

phases:
  install:
    commands:
      - echo noop
  pre_build:
    commands:
      - echo noop
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
