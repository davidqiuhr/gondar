version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - cat src/main.cc
      - make -C /opt/gondar build-gondar test
  post_build:
    commands:
      - echo Build completed on `date`
